# Клиент module_template как отдельный SPA (эталон standalone).

Скопируйте [`core/client/standalone-template`](../../../core/client/standalone-template/)
в новый git-репозиторий. Манифест — [`../client/federation-entry.js`](../client/federation-entry.js).

В монорепо:

```bash
ergoms client-build-standalone --module=module_template
```
